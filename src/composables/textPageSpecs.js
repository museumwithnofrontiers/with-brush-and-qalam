// This exhibition's `TextPageView` pages: legacy rendered credits and nothing
// else, so there is nothing generic to inherit — the entry is this website's
// own, and the platform view supplies the back link and the prose styling.
export const creditsSpec = {
  body: 'withBrushAndQalam.credits.body',
  back: true,
}
